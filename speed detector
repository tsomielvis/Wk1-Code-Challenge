if (speed < 70) {
    print out "Ok";
   }else if (speed >70 for every 5km/s the driver will get an automatic text message via registered phone number in the National Transport and Safety Authority  displaying 1 demerit point for each offense commited regardless the number of times.) {
    print out "Overspeeding";
   }
